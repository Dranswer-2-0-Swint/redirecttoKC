server:
  port: 8888
  tomcat:
    threads:
      max: 1000 # 생성할 수 있는 thread의 총 개수
      min-spare: 250 # 항상 활성화 되어있는(idle) thread의 개수
    max-connections: 8192 # 수립가능한 connection의 총 개수
    accept-count: 250 # 작업큐의 사이즈
    connection-timeout: 20000 # timeout 판단 기준 시간, 20초

spring:
  application:
    name: dranswer
  datasource:
    #driver-class-name: org.postgresql.Driver
    #url: jdbc:postgresql://192.168.85.134:5432/postgres
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:postgres}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:ttt333qqq}

keycloak:
  enabled: true
  realm: service-user-dev
  auth-server-url: http://27.96.130.179:5010/auth
  ssl-required: external
  resource: service-portal
  credentials:
    secret: v3TSB8e6Kd6Yb6gJqmSwcvJA91ERO982
  use-resource-role-mappings: true
  bearer-only: true
  
auth:
  #callback-url: http://192.168.85.132:8888
  callback-url: http://${LOCAL_HOST:localhost}:${LOCAL_PORT:8888}
